program Project1;

uses
  Forms,
  Unit1 in 'Unit1.pas' {ftiket},
  Unit2 in 'Unit2.pas' {fpencarian};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(Tftiket, ftiket);
  Application.CreateForm(Tfpencarian, fpencarian);
  Application.Run;
end.
